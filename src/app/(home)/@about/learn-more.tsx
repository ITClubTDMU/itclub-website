import React from "react";

import { Button } from "@/components/ui/button";

const LearnMore = () => {
  return <Button className="] rounded-full text-md uppercase">Xem thêm</Button>;
};

export default LearnMore;
